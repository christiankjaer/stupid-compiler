# The stupid language is very dynamic
fun dyn(x) =
    if is_char(x)
    then 1
    else if is_int(x)
         then 2
         else if is_unit(x)
              then 3
              else if is_bool(x)
                   then 4
                   else 5


in dyn('c') + dyn(1) + dyn(()) + dyn(true)
